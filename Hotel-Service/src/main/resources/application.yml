server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/HotelService?createIfNotExists=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true